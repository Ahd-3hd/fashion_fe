import React from "react";
import styled from "styled-components";

const NavbarWrapper = styled.nav`
  position: fixed;
  right: 0;
  top: 0;
`;

const Navbar = () => {
  return <NavbarWrapper>hi navbar</NavbarWrapper>;
};

export default Navbar;
